{{#each model as |heartbeat|}}
  <tr>
    <td>
      {{heartbeat.owner}}
    </td>
    <td>
      {{heartbeat.created}}
    </td>
    <td>
      {{heartbeat.beats}}
    </td>
    <td>
      {{heartbeat.is_critical}}
    </td>
  </tr>
{{/each}}
